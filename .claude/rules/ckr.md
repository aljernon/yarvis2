# Core Knowledge Repository (CKR)

`core_knowledge/` is a symlink to `../memory`, which is a **separate git repo**. When committing or pushing CKR changes, always check `git status` and commit/push in the memory repo independently from the main yarvis2 repo.
